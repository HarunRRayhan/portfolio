# Stripe LIVE keys for paid consultations (harun.dev)
#
# Real file (gitignored): .secrets/stripe-live.env
#   cp .secrets/stripe-live.env.example .secrets/stripe-live.env
#
# Never commit stripe-live.env. Only *.example files under .secrets/ are tracked.
#
# Prefer Railway env for production; keep a local copy only if you need CLI access.
# Keys: https://dashboard.stripe.com/apikeys (Test mode OFF)

STRIPE_KEY=
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=
