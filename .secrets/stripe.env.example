# Stripe TEST / sandbox for paid consultations (/book)
#
# Real file (gitignored): .secrets/stripe.env
#   cp .secrets/stripe.env.example .secrets/stripe.env
#
# Never commit stripe.env. Only *.example files under .secrets/ are tracked.
#
# Keys: https://dashboard.stripe.com/test/apikeys
# Webhook (prod): https://harun.dev/stripe/webhook  (checkout.session.completed)
# Local: stripe listen --forward-to localhost:8321/stripe/webhook
#
# Live mode keys go in stripe-live.env, not here.

STRIPE_KEY=
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=
